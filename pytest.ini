[pytest]
markers =
    unit: fast deterministic tests with local stubs/mocks only
    smoke: lightweight environment and wiring checks
    integration: tests that touch real external services or are intended for dedicated integration lanes
